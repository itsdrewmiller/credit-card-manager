<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Mac App Store requires the App Sandbox. The database lives in the
         app container; CSV/PDF import and export go through user-selected
         open/save dialogs; the weekly offer-feed refresh needs outbound
         network. -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Lets the Electron helper processes share the sandbox container. -->
    <key>com.apple.security.application-groups</key>
    <array>
      <string>B676HVHBWG.com.searchlight.cardmanager</string>
    </array>
  </dict>
</plist>
